version: '3'
services:
  node:
    build:
      context: .
      dockerfile: "./docker/node/Dockerfile"
    tty: true
    environment:
      NODE_ENV: "development"
    ports:
      - "3000:3000"
    volumes:
      - "./src:/app"
